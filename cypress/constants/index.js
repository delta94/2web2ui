export const IS_HIBANA_ENABLED = Cypress.env('DEFAULT_TO_HIBANA') === true;

export const USERNAME = 'mockuser'; // When incorporating e2e tests, these could be optionally derived from an env variable

export const PASSWORD = 'abcdefghijklmnop'; // When incorporating e2e tests, these could be optionally derived from an env variable
